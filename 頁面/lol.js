var word = document.getElementById(type)
var button = addEventListener(btn)
var chat = document.getElementById(y)
function send() {
    document.getElementById("type")
    chat.innerHTML = chat.innerHTML + `
    <h4>${word.value}</h4>`; 
  }
